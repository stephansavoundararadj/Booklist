{% extends "::base.html.twig" %}

{% block body %}
{{ parent() }}

<h3>Information sur le roman</h3>

<div class="panel panel-default">
	
	<div class="panel-heading">
		<h4>Liste des différentes informations</h4>
	</div>
	
	<div class="panel-body">
		<table class="table table-striped nowrap-table">
			<thead>
				<tr>
					<th>Titre</th>
					<th>Auteur</th>
					<th>Date de publication</th>
					<th>Description</th>
					<th>Nombre de pages</th>
				</tr>
			</thead>
			
			<tbody>
				<tr>
					<td>{{ info["titre"] }}</td>
					<td>{{ info["auteur"] }}</td>
					<td>{{ info["date_publication"] }}</td>
					<td>{{ info["description"] }}</td>
					<td>{{ info["nombre_pages"] }}</td>
				</tr>
			</tbody>
		</table>
	</div>
</div>

<br/><br/>
<a href="{{ path('book_add') }}" class="btn btn-primary">Retour</a>

<br/><br/>
<a href="{{ path('book_validate', {'book_isbn': info["isbn"]} ) }}" class="btn btn-primary">Valider</a>

{% endblock body %}



